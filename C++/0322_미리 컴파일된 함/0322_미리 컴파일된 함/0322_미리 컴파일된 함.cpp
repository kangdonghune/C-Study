// 0322_미리 컴파일된 함.cpp : 콘솔 응용 프로그램에 대한 진입점을 정의합니다.
//

#include "stdafx.h" //미리 컴파일 된 헤더는 반드시 위에 있어야 한다.
//<> 와 "" 의 차이는 <>는 표준 경로에 포함되어 있을때-표준경로: 마이크로 소프트에서 배포한 폴더 안
//표준 경로에서 벗어나 있을 떄 -개인적으로 프로젝트에서 생성했을 때.
//헤더에서 함수를 선언만 하고 별도의 cpp에서 정의하지 않을 경우 실행 시 에러가 난다.


int main()
{
    return 0;
}

